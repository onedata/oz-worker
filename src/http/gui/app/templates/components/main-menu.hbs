{{#each menuItems as |item|}}
  {{#if session.isAuthenticated}}
    {{#if item.showAuthenticated}}
      {{main-menu-item linkTo=item.linkTo titleI18n=item.titleI18n}}
    {{/if}}
  {{else}}
    {{#if item.showUnauthenticated}}
      {{main-menu-item linkTo=item.linkTo titleI18n=item.titleI18n}}
    {{/if}}
  {{/if}}
{{/each}}
