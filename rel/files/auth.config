%% This is a dummy auth.config file so that rebar won't fail while trying to copy it to the release package.
%% By default it is empty, but if the user has access to onedev repo, it will be exchanged for working config
%% with test apps.